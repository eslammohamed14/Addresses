import theme, {COLORS, SIZES} from './theme';
import images from './images';
import icons from './icons';
import LocalStorage from './LocalStorage';

export {theme, COLORS, SIZES, images, icons, LocalStorage};
